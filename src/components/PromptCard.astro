---
const { title, description, category, tags, href } = Astro.props;
const tagList = (tags || []).join(",");
---

<a
  href={href}
  data-prompt-card
  data-title={title}
  data-description={description}
  data-category={category}
  data-tags={tagList}
  class="group relative flex h-full flex-col justify-between rounded-2xl border border-zinc-200 bg-white/70 p-6 shadow-[0_0_0_1px_rgba(255,255,255,0.4)] transition hover:-translate-y-1 hover:border-zinc-300 hover:bg-white dark:border-zinc-800 dark:bg-zinc-950/40 dark:shadow-[0_0_0_1px_rgba(24,24,27,0.6)] dark:hover:border-zinc-700"
>
  <div class="space-y-3">
    <div class="flex items-center justify-between text-xs uppercase tracking-[0.3em] text-zinc-500 dark:text-zinc-400">
      <span>{category}</span>
      <span class="text-zinc-400 dark:text-zinc-500">Prompt</span>
    </div>
    <h2 class="text-xl font-semibold text-zinc-900 transition group-hover:text-zinc-950 dark:text-zinc-100">
      {title}
    </h2>
    <p class="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400">{description}</p>
  </div>
  <div class="mt-6 flex flex-wrap gap-2">
    {tags?.map((tag) => (
      <span class="rounded-full border border-zinc-200 px-3 py-1 text-[11px] uppercase tracking-[0.2em] text-zinc-600 dark:border-zinc-800 dark:text-zinc-400">
        {tag}
      </span>
    ))}
  </div>
</a>
